# Copy to `.env` at the repo root. Never commit the real file.
# Next, Express, and Prisma all read this one file. Only NEXT_PUBLIC_* reaches the browser.

NEXT_PUBLIC_API_URL=http://localhost:4000
DATABASE_URL=
DIRECT_URL=
JWT_SECRET=
ENCRYPTION_KEY=
PORT=4000
FRONTEND_URL=http://localhost:3000
